{
  "name": "@kitsy/cnos-var-rpc",
  "version": "1.17.0",
  "description": "gRPC (rpc) transport module for CNOS runtime variables (var.*): Pull + Subscribe VarSourceProvider client and an embeddable VarService server.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./proto/*": "./proto/*"
  },
  "files": [
    "dist",
    "proto"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0",
    "@grpc/proto-loader": "^0.7.10",
    "@kitsy/cnos-var-server": "workspace:*"
  },
  "devDependencies": {
    "@kitsy/cnos-core": "workspace:*",
    "@kitsy/cnos-var-testkit": "workspace:*"
  },
  "scripts": {
    "build": "rimraf dist && tsup --config tsup.config.ts",
    "clean": "rimraf dist",
    "dev": "tsup --config tsup.config.ts --watch",
    "lint": "eslint src test",
    "prepack": "pnpm build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
