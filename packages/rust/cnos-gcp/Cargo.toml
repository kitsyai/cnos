[package]
name = "cnos-gcp"
version = "1.16.0"
edition = "2021"
description = "CNOS GCP Secret Manager vault provider for Rust"
license = "MIT"
repository = "https://github.com/kitsyai/cnos"

[dependencies]
cnos = { path = "../cnos", version = "1.12.2" }
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde_json = "1"
