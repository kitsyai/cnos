[package]
name = "cnos-firebase"
version = "1.11.4"
edition = "2021"
description = "CNOS Firebase Secrets vault provider for Rust (wraps GCP)"
license = "MIT"
repository = "https://github.com/kitsyai/cnos"

[dependencies]
cnos = { path = "../cnos" }
cnos-gcp = { path = "../cnos-gcp" }
serde_json = "1"
