# @kitsy/cnos

Developer-friendly CNOS runtime assembly. It prewires the official v1 plugins on top of `@kitsy/cnos-core`, exposes the main `createCnos(...)` entry point for app code, and re-exports first-party helpers such as `@kitsy/cnos/plugin/vite`.
