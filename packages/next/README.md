# @kitsy/cnos-next

Next.js integration for CNOS.

It resolves CNOS public config during Next config evaluation and merges the exported values into the `env` block returned from `next.config.*`, using the Next public convention.
