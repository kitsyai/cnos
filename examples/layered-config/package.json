{
  "name": "@kitsy/cnos-example-layered-config",
  "private": true,
  "type": "module",
  "dependencies": {
    "@kitsy/cnos": "workspace:*",
    "@kitsy/cnos-plugin-env-export": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "clean": "rimraf dist",
    "dev": "tsup src/index.ts --watch --format esm --clean",
    "lint": "eslint src test",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
